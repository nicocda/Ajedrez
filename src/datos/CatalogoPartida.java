package datos;
import java.sql.ResultSet;
import java.sql.SQLException;
//import conexion.DataConnection;

public class CatalogoPartida {

/*	public boolean existePartida(int j1, int j2) throws SQLException
	{
	DataConnection con = new DataConnection();
	ResultSet resultado = null;
	resultado = con.getQuery("select * "
			+ "from partida p "
			+ "where (blanco="+j1+" and negro="+j2+") or (blanco="+j2+" and negro="+j1+") ");
	return (resultado.next());
	}

	public void agregarPartida(int j1, int j2) {
		
		DataConnection con = new DataConnection();
		con.setQuery("INSERT INTO partida (blanco, negro) VALUES ("+j1+", "+j2+")");
		
		}*/
	
}
